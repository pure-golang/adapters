Q: не хватает (плюс поддержка разных типов через generic):
```go
// env/env.go

func getEnv(key, defaultValue string) string {
  if value := os.Getenv(key); value != "" {
    return value
  }
  return defaultValue
}
```

A: `default` прописывается в тегах (типизируется прозрачно)

Q: обнаружил sqlx, хотелось бы sqlc. знакомый евангелист sqlc https://habr.com/ru/companies/uzum/articles/890654/

A: 1) Sqlc генерирует код по sql. А что делать если нужен query builder? Для составления фильтров к примеру? 2) Sqlx и pgx только выполняют запросы при этом ты сам выбираешь как писать sql. Sqlx удобный, pgx более зрелый и тоже в целом ничего. То есть по сути они обеспечивают поддержку соединений и выполнения. 3) обычно использую https://github.com/Masterminds/squirrel 
старый, проверенный, ничего не навязывает, работает с любым *sql.DB
в адаптерах его нет т.к. он возникает на уровне репо. пока этот слой меня меньше всего интересует, но и до него доберемся. скидывайте свои sql builders

Q: у меня есть свой велосипед про структурированные логи https://habr.com/ru/articles/912788/ (вдохновлённый докладом https://www.youtube.com/watch?v=p9XiOOU52Qw)

A: Если следует интерфейсу slog.Logger (а он вроде следует) то просто докидывай поставщика услуги. это будет использоваться везде, так что хорошо бы сделать хорошо) кст посмотри для локальной разработки там devslog. Покрасивее в выводе. devslog почти такой же (как мой pplog), но в проде скорее всего будет std т.к. он без спецсимволов (colors). логи забираются сайдкаром в k8s из пода и пушатся в хранилище которое не знает о форматировании как и та часть которая будет отображать. (не так немного, логи кладутся на воркер машину по длинному урлу, а компоненты эти логи забирают в своё хранилище, и дальше используя шаблонизатор раскладывают по полям. компоненты не k8s, а observability. Буду ставить vector-agent+vl cluster+grafana)

Q: gorilla/mux избыточен вроде

A: mux сейчас затащили в стандартную либу. главное от туда интерфейс. надо будет переделать. спс за коммент

Q: 🤖 Это битва двух подходов к интеграционному тестированию в Docker. Если коротко: Testcontainers победил в войне экосистем, но dockertest всё еще жив благодаря своей простоте.

A: dockertest - старые тесты. раньше другого не было